/*
 * Created by Duleep Kodithuwakku on 19-05-2019.
 */
const to = {
    type: "string"
    // pattern: "^[0-9]{8}$"
}

const content = {
    type: "string"
}

const subject = {
    type: "string"
}

module.exports = {
    to,
    content,
    subject
}
